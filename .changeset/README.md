# Changesets

This directory is managed by [changesets](https://github.com/changesets/changesets). Each `.md` file
(other than this one) describes a set of package version bumps and changelog entries for the next
release. Run `pnpm changeset` to add one; `pnpm version-packages` to apply them; `pnpm release` to
publish.
