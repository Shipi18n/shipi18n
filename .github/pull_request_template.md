## What this changes

<!-- One or two sentences. What behaviour is different afterwards? -->

## Why

<!-- The problem this solves. Link an issue if there is one. -->

## How it was verified

<!-- `pnpm test` is the baseline. If you ran a real translation, say which
     provider, language pair and input — that is the part tests cannot cover. -->

- [ ] `pnpm test` passes
- [ ] Tests added or updated for the behaviour change
- [ ] Changeset added (`pnpm changeset`) if this is user-visible
